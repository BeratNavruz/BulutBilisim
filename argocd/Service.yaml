apiVersion: v1
kind: Service
metadata:
  name: bulut-bilisim-project-service
spec:
  selector:
    name: bulut-bilisim-project-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer